{
    "express": {
        "view cache": false
    },
    "middleware": {
        "shutdown": {
            // Clean shutdowns aren"t necessary in `dev` mode
            "enabled": false
        },
        "request": {
            "enabled": true
        },
        "render": {
            "enabled": true
        }
    }
}